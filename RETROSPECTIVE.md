# セッション振り返り

## やったこと

仕様書 (PSN-81) をもとに、ジュビロ磐田ダッシュボードの Phase 1 を実装した。

- Streamlit + Plotly によるダッシュボード UI（3タブ）
- データ取得クライアント・変換層・fetch スクリプト
- Dockerfile（Cloud Run 用）
- GitHub Actions（日次データ更新・Cloud Run デプロイ）
- uv によるパッケージ管理

---

## 課題・失敗したこと

### 1. データソース選定を実装前に検証しなかった

| 試したAPI | 結果 | 原因 |
|-----------|------|------|
| football-data.org | Jリーグ非対応 | 無料枠は欧州+南米13リーグのみ |
| RapidAPI (api-football) | サブスクリプション画面が見つからない | RapidAPI の UI 変更 |
| api-football.com (直接) | 2025/2026 シーズン非対応 | 無料枠は 2022〜2024 のみ |

→ **最初に「Jリーグのデータが無料で取れるか」を検証してから設計すべきだった。**

### 2. 開発環境のIP制限を把握していなかった

Claude Code のリモート実行環境（クラウドIP）はすべての外部スポーツAPIにブロックされる。
これを把握していれば、最初からローカル実行 or GitHub Actions での検証を前提に設計できた。

### 3. コマンドの変遷が多く、ユーザーに負担をかけた

```
pip install → uv pip install --system → uv pip install → uv sync
```

uv の正しい使い方（`uv sync` / `uv run`）を最初から選定すべきだった。

### 4. デフォルトシーズンが無意味

2026年時点で `--season 2024` をデフォルトにしても、データが古いだけで実用性がない。
無料プランの制約を先に把握した上でデータソースを選ぶべきだった。

---

## 次回の改善点

### データ面

- [ ] **有料プランの検討**: api-football.com の Standard プラン（$10/月程度）で現在シーズンが取れる
- [ ] **代替データソース調査**: Jリーグ公式サイトのスクレイピング可否・利用規約の確認
- [ ] **StatsBomb 以外のオープンデータ調査**: Kaggle 等の公開データセット

### 設計面

- [ ] **データソース検証を実装前に行う**: curl 一発で取れるか確認してから設計
- [ ] **シーズン管理の改善**: `--season` を廃止し、APIから自動的に最新シーズンを取得する
- [ ] **データが空のときの UI**: 「データ取得中」「無料プラン制限」等のエラーメッセージを丁寧に出す

### 運用面

- [ ] **GitHub Secrets の設定**: `API_FOOTBALL_KEY` / `GCP_PROJECT_ID` / `GCP_SA_KEY` の登録
- [ ] **Cloud Run へのデプロイ検証**: 実際にデプロイして URL を確認する
- [ ] **PR のマージ**: `claude/check-spec-document-9p6kq` → `main`

---

## 現状

| 項目 | 状態 |
|------|------|
| Streamlit UI | ✅ 実装済み（モックデータで動作確認済み） |
| データ取得スクリプト | ✅ 実装済み（ローカルから 2024 シーズンデータ取得可） |
| Cloud Run デプロイ設定 | ✅ workflow 実装済み（未検証） |
| 実データでの動作確認 | ❌ 未実施 |
| 本番デプロイ | ❌ 未実施 |
